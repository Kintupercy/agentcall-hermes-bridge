node_modules/
dist/
.wrangler/
*.tsbuildinfo

# Local env / secrets — never commit
.env
.env.local
.dev.vars
consumer/consumer.env
consumer/config.json
consumer/brains/hermes_brain.sh

# Consumer runtime state (counters + the replied-message set)
consumer/state.json
__pycache__/
*.pyc

# bootstrap.sh per-run state: unrecoverable secrets, rollback commands, and
# the config each run deployed with. Namespaced by account + worker name so
# bootstrapping a second client never overwrites the first one's recovery data.
.bootstrap/
wrangler.jsonc.bak
