node_modules/
dist/
.wrangler/
*.tsbuildinfo

# Local env / secrets — never commit
.env
.env.local
.dev.vars
consumer/consumer.env
consumer/config.json
consumer/brains/hermes_brain.sh

# Consumer runtime state (counters + the replied-message set)
consumer/state.json
__pycache__/
*.pyc

# bootstrap.sh output: generated secrets, rollback commands, config backup
.bootstrap-secrets
.bootstrap-rollback
wrangler.jsonc.bak
